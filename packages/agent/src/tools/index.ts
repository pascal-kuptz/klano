// Tool implementations live here once we run the agent in tool-calling mode
// (v0.6 — searchVenues, scheduleFollowUp, classifyInboundEmail). For now,
// draftOutreachEmail runs as a direct streamText call — see ../draft-outreach.
export {};
